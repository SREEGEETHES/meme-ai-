# This project uses browser localStorage for keys.
# No server env vars are required for the Vercel/Netlify demo.

# Vercel Blob — for permanent meme storage (optional but recommended)
# Get token at https://vercel.com/docs/storage/vercel-blob
# BLOB_READ_WRITE_TOKEN=
